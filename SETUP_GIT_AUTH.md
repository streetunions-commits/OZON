# 🔐 Настройка Git без постоянного ввода пароля

## ✅ Рекомендуемый способ: Personal Access Token

1. **Создайте токен на GitHub:**
   - Перейдите: https://github.com/settings/tokens
   - Нажмите "Generate new token (classic)"
   - Выберите срок действия: "No expiration"
   - Выберите права: `repo` (все галочки в разделе repo)
   - Нажмите "Generate token"
   - **СКОПИРУЙТЕ токен** (вы увидите его только один раз!)

2. **Настройте Git:**
   ```powershell
   # В PowerShell выполните:
   git config --global credential.helper manager-core
   ```

3. **Первый push с токеном:**
   ```powershell
   git push
   ```

   **При запросе:**
   - Username: ваш_github_username
   - Password: вставьте_токен (НЕ пароль, а токен!)

4. **Готово!** Больше пароль не будет запрашиваться.

---

## 🔧 Альтернативный способ: SSH ключи

1. **Генерируем SSH ключ:**
   ```powershell
   ssh-keygen -t ed25519 -C "your_email@example.com"
   ```
   Нажмите Enter 3 раза (без пароля)

2. **Копируем публичный ключ:**
   ```powershell
   cat ~/.ssh/id_ed25519.pub | clip
   ```

3. **Добавляем на GitHub:**
   - Перейдите: https://github.com/settings/keys
   - Нажмите "New SSH key"
   - Вставьте ключ из буфера обмена
   - Сохраните

4. **Меняем remote на SSH:**
   ```powershell
   git remote set-url origin git@github.com:YOUR_USERNAME/YOUR_REPO.git
   ```

5. **Тестируем:**
   ```powershell
   ssh -T git@github.com
   git push
   ```

---

## ⚡ Быстрое решение: Сохранить пароль в config (НЕ БЕЗОПАСНО!)

```powershell
git config --global credential.helper store
git push
# Введите логин и пароль ОДИН раз - они сохранятся в ~/.git-credentials
```

**⚠️ Внимание:** Пароль хранится в открытом виде!

---

## 🔍 Проверка текущих настроек

```powershell
git config --global credential.helper
git remote -v
```
