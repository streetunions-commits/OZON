#!/bin/bash
# ============================================================================
# АВТОМАТИЧЕСКАЯ СИНХРОНИЗАЦИЯ OZON TRACKER
# ============================================================================
#
# Назначение: Автоматический запуск синхронизации данных каждые 6 часов
#
# Использование: Запускается через cron
#   0 */6 * * * /root/OZON/cron_sync.sh >> /var/log/ozon-tracker/cron.log 2>&1
#
# Почему Python напрямую, а не HTTP API:
#   - Синхронизация не прерывается при рестарте веб-сервера
#   - Не требует авторизации
#   - Использует файловую блокировку против параллельных запусков
#
# ============================================================================

set -e

# Путь к проекту
OZON_DIR="/root/OZON"
VENV_PYTHON="${OZON_DIR}/venv/bin/python3"
SYNC_SCRIPT="${OZON_DIR}/sync_standalone.py"

# Логирование с временной меткой
log_info() {
    echo "[$(date '+%Y-%m-%d %H:%M:%S')] $1"
}

log_info "🔄 Запуск автоматической синхронизации..."

# Проверяем, что скрипт существует
if [ ! -f "$SYNC_SCRIPT" ]; then
    log_info "❌ Скрипт синхронизации не найден: $SYNC_SCRIPT"
    exit 1
fi

# Запускаем Python скрипт напрямую (не через HTTP API)
cd "$OZON_DIR"
"$VENV_PYTHON" "$SYNC_SCRIPT"
exit_code=$?

if [ $exit_code -eq 0 ]; then
    log_info "✅ Синхронизация завершена успешно"
else
    log_info "❌ Синхронизация завершилась с ошибкой (код: $exit_code)"
    exit $exit_code
fi

log_info "Готово!"
