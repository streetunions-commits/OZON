# Изменения Ozon Performance API от 20.01.2026

> ⚠️ **КРИТИЧЕСКИ ВАЖНО**: С 20 января 2026 года Performance API стал **асинхронным**!

---

## 🔄 Основное изменение: API стал асинхронным

**Раньше:** Запрос сразу возвращал CSV с данными
**Теперь:** Запрос возвращает UUID, нужно проверять статус и скачивать готовый отчёт

### Новый процесс работы:

```
1. Отправить POST запрос → получить UUID
2. Проверить статус формирования отчёта по UUID
3. Скачать готовый отчёт (CSV или ZIP)
```

---

## 📋 Новый эндпоинт: POST /api/client/statistics

### Request Body (application/json)

```json
{
  "campaigns": ["campaign_id_1", "campaign_id_2"],
  "from": "2019-08-24T14:15:22Z",
  "to": "2019-08-24T14:15:22Z",
  "dateFrom": "2019-02-10",
  "dateTo": "2019-02-10",
  "groupBy": "NO_GROUP_BY"
}
```

### Параметры запроса:

| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| `campaigns` | Array<string> | ✅ Да | Список ID кампаний для отчёта |
| `from` | string (RFC 3339) | Нет | Начальная дата (формат: 2019-08-24T14:15:22Z) |
| `to` | string (RFC 3339) | Нет | Конечная дата (формат: 2019-08-24T14:15:22Z) |
| `dateFrom` | string (ГГГГ-ММ-ДД) | Нет | Начальная дата (формат: 2019-02-10) |
| `dateTo` | string (ГГГГ-ММ-ДД) | Нет | Конечная дата (формат: 2019-02-10) |
| `groupBy` | string | Нет | Тип группировки: `NO_GROUP_BY`, `DATE`, `START_OF_WEEK`, `START_OF_MONTH` |

**⚠️ ВАЖНО:** Если заполнены все четыре поля (`from`, `to`, `dateFrom`, `dateTo`), в ответе будет статистика по временному промежутку из полей `dateFrom` и `dateTo`.

**Максимальный период:** 62 дня

---

## 📤 Response (HTTP 200)

```json
{
  "UUID": "0c159c60-ab92-46d9-9a6b-d225dbf5c7b1",
  "vendor": true
}
```

| Поле | Тип | Описание |
|------|-----|----------|
| `UUID` | string | Уникальный идентификатор запроса (для проверки статуса и скачивания) |
| `vendor` | boolean | Если запрашивается отчёт с аналитикой внешнего трафика — true |

---

## 📥 Формат отчёта

### CSV (одна кампания)
Если в запросе `campaigns: ["campaign_id"]` — один элемент, отчёт будет в формате CSV.

### ZIP (несколько кампаний)
Если в запросе `campaigns: ["id1", "id2", "id3"]` — несколько элементов:
- Отчёт будет ZIP архивом
- Каждый файл: `<идентификатор кампании>.csv`

---

## 🎯 Типы группировки (groupBy)

| Значение | Описание |
|----------|----------|
| `NO_GROUP_BY` | Без группировки (по умолчанию) |
| `DATE` | Группировка по дате (по дням) |
| `START_OF_WEEK` | Группировка по неделям |
| `START_OF_MONTH` | Группировка по месяцам |

---

## 🔗 JSON формат отчёта

Чтобы запросить отчёт в формате JSON:

```
POST /api/client/statistics/json
```

---

## 📚 Примеры отчётов по типам кампаний

- **Баннеры** — формат отчёта отличается
- **Видеобаннеры** — формат отчёта отличается
- **Оплата за клик** — формат отчёта отличается

---

## 🎬 Эндпоинт для видеобаннеров: POST /api/client/statistics/video

### Описание

Асинхронный API для получения статистики по видеобаннерам. Формат работы аналогичен основному эндпоинту.

### Request Body (application/json)

```json
{
  "campaigns": ["campaign_id_1", "campaign_id_2"],
  "dateFrom": "2019-02-10",
  "dateTo": "2019-02-10",
  "groupBy": "NO_GROUP_BY"
}
```

### Параметры запроса:

| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| `campaigns` | Array<string> | ✅ Да | Список ID кампаний для отчёта |
| `dateFrom` | string (ГГГГ-ММ-ДД) | ✅ Да | Начальная дата (формат: 2019-02-10) |
| `dateTo` | string (ГГГГ-ММ-ДД) | ✅ Да | Конечная дата (формат: 2019-02-10) |
| `groupBy` | string | Нет | Тип группировки: `NO_GROUP_BY`, `DATE`, `START_OF_WEEK`, `START_OF_MONTH` |

**⚠️ ВАЖНО:** Для видеобаннеров используются **только** `dateFrom` и `dateTo` (не `from/to`).

### Response (HTTP 200)

```json
{
  "UUID": "0c159c60-ab92-46d9-9a6b-d225dbf5c7b1",
  "vendor": true
}
```

### Содержимое отчёта (CSV)

Отчёт содержит следующие метрики:

| Метрика | Описание |
|---------|----------|
| Название | Название кампании |
| Идентификатор кампании | ID кампании |
| Период отчёта | Временной промежуток |
| Идентификатор баннера | ID баннера |
| Показы | Количество показов |
| Клики | Количество кликов |
| Охват | Охват аудитории |
| CTR | Click-Through Rate (процент кликов) |
| Доля видимых показов | Процент видимых показов |
| Досмотры 25% | Количество досмотров до 25% |
| Досмотры 50% | Количество досмотров до 50% |
| Досмотры 75% | Количество досмотров до 75% |
| Досмотры 100% | Количество досмотров до конца |
| Доля досмотров 25% | Процент досмотров до 25% |
| Доля досмотров 50% | Процент досмотров до 50% |
| Доля досмотров 75% | Процент досмотров до 75% |
| Доля досмотров 100% | Процент досмотров до конца |
| Просмотры со звуком | Количество просмотров со звуком |
| Заказы | Количество заказов |
| Расход | Расход на рекламу |

### JSON формат

Чтобы запросить отчёт в формате JSON:

```
POST /api/client/statistics/video/json
```

### Формат отчёта

- **CSV** — если в запросе одна кампания
- **ZIP архив** — если несколько кампаний (имя файла: `<идентификатор кампании>.csv`)

---

## 📦 Эндпоинт для отчётов по заказам: POST /api/client/statistics/attribution

### Описание

Асинхронный API для получения отчёта по заказам на баннеры. Формат работы аналогичен основному эндпоинту.

### Request Body (application/json)

```json
{
  "campaigns": ["campaign_id_1", "campaign_id_2"],
  "from": "2019-08-24T14:15:22Z",
  "to": "2019-08-24T14:15:22Z",
  "dateFrom": "2019-02-10",
  "dateTo": "2019-02-10",
  "groupBy": "NO_GROUP_BY"
}
```

### Параметры запроса:

| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| `campaigns` | Array<string> | ✅ Да | Список ID кампаний для отчёта |
| `from` | string (RFC 3339) | Нет | Начальная дата (формат: 2019-08-24T14:15:22Z) |
| `to` | string (RFC 3339) | Нет | Конечная дата (формат: 2019-08-24T14:15:22Z) |
| `dateFrom` | string (ГГГГ-ММ-ДД) | Нет | Начальная дата (формат: 2019-02-10) |
| `dateTo` | string (ГГГГ-ММ-ДД) | Нет | Конечная дата (формат: 2019-02-10) |
| `groupBy` | string | Нет | Тип группировки: `NO_GROUP_BY`, `DATE`, `START_OF_WEEK`, `START_OF_MONTH` |

**⚠️ ВАЖНО:** Если заполнены все четыре поля (`from`, `to`, `dateFrom`, `dateTo`), в ответе будет статистика по временному промежутку из полей `dateFrom` и `dateTo`.

**Максимальный период:** 62 дня

### Response (HTTP 200)

```json
{
  "UUID": "0c159c60-ab92-46d9-9a6b-d225dbf5c7b1",
  "vendor": true
}
```

### Содержимое отчёта (CSV)

Отчёт содержит следующие данные:

| Поле | Описание |
|------|----------|
| Название | Название кампании |
| Идентификатор кампании | ID кампании |
| Идентификатор баннера | ID баннера |
| Период отчёта | Временной промежуток |
| SKU | SKU товара |
| Название товара | Наименование товара |
| Количество заказов | Число заказов |
| Выручка в рублях | Выручка от продаж |
| Количество заказов модели | Число заказов модели товара |
| Выручка с заказов модели | Выручка от заказов модели |
| Тип атрибуции | Модель атрибуции |

### JSON формат

Чтобы запросить отчёт в формате JSON:

```
POST /api/client/statistics/attribution/json
```

### Формат отчёта

- **CSV** — если в запросе одна кампания
- **ZIP архив** — если несколько кампаний (имя файла: `<идентификатор кампании>.csv`)

---

## 📊 Эндпоинт для медийных кампаний: GET /api/client/statistics/campaign/media

### Описание

**⚠️ ВАЖНО:** Этот эндпоинт **СИНХРОННЫЙ** (не асинхронный)! Сразу возвращает CSV, а не UUID.

API для получения статистики по медийным кампаниям.

### Query Parameters

| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| `campaignIds` | Array<string> | Нет | Список ID кампаний. Если пустой — статистика по всем кампаниям |
| `from` | string (RFC 3339) | Нет | Начальная дата (формат: 2019-08-24T14:15:22Z) |
| `to` | string (RFC 3339) | Нет | Конечная дата (формат: 2019-08-24T14:15:22Z) |
| `dateFrom` | string (ГГГГ-ММ-ДД) | Нет | Начальная дата (формат: 2019-02-10) |
| `dateTo` | string (ГГГГ-ММ-ДД) | Нет | Конечная дата (формат: 2019-02-10) |

**⚠️ ВАЖНО:** Если заполнены все четыре поля (`from`, `to`, `dateFrom`, `dateTo`), в ответе будет статистика по временному промежутку из полей `dateFrom` и `dateTo`.

### Пример запроса

```http
GET /api/client/statistics/campaign/media?dateFrom=2024-08-01&dateTo=2024-08-01&campaignIds=11037395&campaignIds=11037396 HTTP/1.1
Host: api-performance.ozon.ru:443
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
Content-Type: application/json
```

### Response (HTTP 200)

```json
{
  "contentType": "string"
}
```

**Формат ответа:** CSV (сразу, без UUID)

### Содержимое отчёта (CSV)

Отчёт содержит следующие данные:

| Поле | Описание |
|------|----------|
| Идентификатор кампании | ID кампании |
| Название кампании | Наименование кампании |
| Формат кампании | Тип кампании |
| Статус кампании | Текущий статус |
| Дневной бюджет в рублях | Бюджет на день |
| Бюджет в рублях | Общий бюджет |
| Расход в рублях | Фактический расход |
| Показы | Количество показов |
| Клики | Количество кликов |
| Средняя цена 1000 показов | CPM (Cost Per Mille) |
| CTR | Click-Through Rate (клики/показы) |
| Средняя цена клика | CPC (Cost Per Click) |
| Заказы в штуках | Количество заказов |
| Заказы после просмотра | Заказы view-through |
| Сумма заказов после просмотра | Выручка view-through |
| ДРР | Доля рекламных расходов |
| Сумма заказов | Общая выручка |
| Тип оплаты | Модель оплаты |
| Тип бюджета | Тип бюджета кампании |

### JSON формат

Чтобы запросить отчёт в формате JSON:

```
GET /api/client/statistics/campaign/media/json
```

### Отличия от других эндпоинтов

- ✅ **Синхронный** — сразу возвращает CSV
- ✅ **GET запрос** — параметры в query string
- ✅ **Без UUID** — не нужно проверять статус
- ✅ **campaignIds необязателен** — можно получить все кампании

---

## 💰 Эндпоинт для расходов кампаний: GET /api/client/statistics/expense

### Описание

**✅ ХОРОШИЕ НОВОСТИ:** Этот эндпоинт **СИНХРОННЫЙ** и **ВСЁ ЕЩЁ РАБОТАЕТ**!

API для получения статистики по расходам кампаний. Это тот самый эндпоинт, который используется в текущем коде.

### Query Parameters

| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| `campaignIds` | Array<string> | Нет | Список ID кампаний. Если пустой — статистика по всем кампаниям |
| `dateFrom` | string (ГГГГ-ММ-ДД) | Нет | Начальная дата (формат: 2019-02-10). По умолчанию: последние 7 дней |
| `dateTo` | string (ГГГГ-ММ-ДД) | Нет | Конечная дата (формат: 2019-02-10). По умолчанию: последние 7 дней |

**⚠️ ВАЖНО:** Если `dateFrom` и `dateTo` не указаны, в ответе будет статистика за **последние 7 дней**.

### Пример запроса

```http
GET /api/client/statistics/expense?campaigns=48852&dateFrom=2019-02-10&dateTo=2020-02-10 HTTP/1.1
Host: api-performance.ozon.ru:443
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
Content-Type: application/json
```

### Response (HTTP 200)

```json
{
  "contentType": "string"
}
```

**Формат ответа:** CSV (сразу, без UUID)

### Содержимое отчёта (CSV)

Отчёт содержит следующие данные:

| Поле | Описание |
|------|----------|
| Идентификатор кампании | ID кампании |
| Дата | Дата расхода |
| Название | Название кампании |
| Расход | Общий расход в рублях |
| Расход с абонентского счёта | Расход с абонентского счёта |
| Расход бонусов | Расход бонусов |

### JSON формат

Чтобы запросить отчёт в формате JSON:

```
GET /api/client/statistics/expense/json
```

### Статус эндпоинта

- ✅ **Работает** — эндпоинт всё ещё функционирует после изменений от 20.01.2026
- ✅ **Синхронный** — сразу возвращает CSV
- ✅ **Обратная совместимость** — старый код продолжает работать
- ⚠️ **Ограниченная функциональность** — новые асинхронные эндпоинты предоставляют больше данных

### Использование в текущем коде

Этот эндпоинт используется в функции `load_adv_spend_by_sku()`:

```python
expense_url = "https://api-performance.ozon.ru/api/client/statistics/expense"
r = requests.get(expense_url, headers=headers, params=params)
csv_content = r.text  # Сразу получаем CSV
```

**Вывод:** Текущий код **продолжает работать**, изменения не требуются (но можно мигрировать на новые эндпоинты для расширенной функциональности).

---

## 📅 Эндпоинт для дневной статистики: GET /api/client/statistics/daily

### Описание

**✅ ХОРОШИЕ НОВОСТИ:** Этот эндпоинт **СИНХРОННЫЙ** и **ВСЁ ЕЩЁ РАБОТАЕТ**!

API для получения дневной статистики по кампаниям.

### Query Parameters

| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| `campaignIds` | Array<string> | Нет | Список ID кампаний. Если пустой — статистика по всем кампаниям |
| `dateFrom` | string (ГГГГ-ММ-ДД) | Нет | Начальная дата (формат: 2019-02-10). По умолчанию: последние 7 дней |
| `dateTo` | string (ГГГГ-ММ-ДД) | Нет | Конечная дата (формат: 2019-02-10). По умолчанию: последние 7 дней |

**⚠️ ВАЖНО:** Если `dateFrom` и `dateTo` не указаны, в ответе будет статистика за **последние 7 дней**.

### Пример запроса

```http
GET /api/client/statistics/daily?campaigns=48852&dateFrom=2019-02-10&dateTo=2020-02-10 HTTP/1.1
Host: api-performance.ozon.ru:443
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
Content-Type: application/json
```

### Response (HTTP 200)

```json
{
  "contentType": "string"
}
```

**Формат ответа:** CSV (сразу, без UUID)

### Содержимое отчёта (CSV)

Отчёт содержит следующие данные:

| Поле | Описание |
|------|----------|
| Идентификатор кампании | ID кампании |
| Название кампании | Наименование кампании |
| Дата | Дата статистики |
| Показы | Количество показов |
| Клики | Количество кликов |
| Расход в рублях | Расход на рекламу |
| Заказы в штуках | Количество заказов |
| Заказы в рублях | Выручка от заказов |

### JSON формат

Чтобы запросить отчёт в формате JSON:

```
GET /api/client/statistics/daily/json
```

### Статус эндпоинта

- ✅ **Работает** — эндпоинт всё ещё функционирует после изменений от 20.01.2026
- ✅ **Синхронный** — сразу возвращает CSV
- ✅ **Обратная совместимость** — можно использовать для получения дневной статистики

---

## 🛍️ Эндпоинт для кампаний "Оплата за клик": GET /api/client/statistics/campaign/product

### Описание

**✅ ХОРОШИЕ НОВОСТИ:** Этот эндпоинт **СИНХРОННЫЙ** и **ВСЁ ЕЩЁ РАБОТАЕТ**!

API для получения статистики по продуктовым кампаниям (Оплата за клик / SKU кампании).

### Query Parameters

| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| `campaignIds` | Array<string> | Нет | Список ID кампаний. Если пустой — статистика по всем кампаниям |
| `from` | string (RFC 3339) | Нет | Начальная дата (формат: 2019-08-24T14:15:22Z) |
| `to` | string (RFC 3339) | Нет | Конечная дата (формат: 2019-08-24T14:15:22Z) |
| `dateFrom` | string (ГГГГ-ММ-ДД) | Нет | Начальная дата (формат: 2019-02-10) |
| `dateTo` | string (ГГГГ-ММ-ДД) | Нет | Конечная дата (формат: 2019-02-10) |

**⚠️ ВАЖНО:** Если заполнены все четыре поля (`from`, `to`, `dateFrom`, `dateTo`), в ответе будет статистика по временному промежутку из полей `dateFrom` и `dateTo`.

### Пример запроса

```http
GET /api/client/statistics/campaign/product?dateFrom=2024-08-01&dateTo=2024-08-07&campaignIds=11037395&campaignIds=11037396 HTTP/1.1
Host: api-performance.ozon.ru:443
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
Content-Type: application/json
```

### Response (HTTP 200)

```json
{
  "contentType": "string"
}
```

**Формат ответа:** CSV (сразу, без UUID)

### Содержимое отчёта (CSV)

Отчёт содержит следующие данные:

| Поле | Описание |
|------|----------|
| Идентификатор кампании | ID кампании |
| Название кампании | Наименование кампании |
| Тип объекта | Тип объекта рекламы |
| Статус кампании | Текущий статус |
| Недельный бюджет в рублях | Бюджет на неделю |
| Бюджет в рублях | Общий бюджет |
| Расход в рублях | Фактический расход |
| Показы | Количество показов |
| Клики | Количество кликов |
| Добавления в корзину | Количество добавлений товара в корзину |
| CTR | Click-Through Rate (клики/показы) |
| Средняя цена клика | CPC (Cost Per Click) |
| Заказы в штуках | Количество заказов |
| Заказы в рублях | Выручка от заказов |
| ДРР | Доля рекламных расходов |
| Тип продвижения | Тип продвижения товара |
| Места размещения | Места размещения для спецразмещения |

### JSON формат

Чтобы запросить отчёт в формате JSON:

```
GET /api/client/statistics/campaign/product/json
```

### Статус эндпоинта

- ✅ **Работает** — эндпоинт всё ещё функционирует после изменений от 20.01.2026
- ✅ **Синхронный** — сразу возвращает CSV
- ✅ **Обратная совместимость** — можно использовать для получения статистики по SKU кампаниям
- 📊 **Подробная статистика** — включает данные о корзине, ДРР, местах размещения

---

## 🛒 Эндпоинт для заказов "Оплата за заказ": POST /api/client/statistic/orders/generate

### Описание

**⚠️ ВАЖНО:** Этот эндпоинт **АСИНХРОННЫЙ**! Возвращает UUID, нужно проверять статус и скачивать отчёт.

API для получения отчёта по заказам в кампаниях "Оплата за заказ" (SEARCH_PROMO).

**⚠️ ОБРАТИТЕ ВНИМАНИЕ:** В пути `/statistic/` без "s" (опечатка в API).

### Request Body (application/json)

```json
{
  "from": "2019-08-24T14:15:22Z",
  "to": "2019-08-24T14:15:22Z"
}
```

### Параметры запроса:

| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| `from` | string (RFC 3339) | ✅ Да | Начальная дата (формат: 2019-08-24T14:15:22Z) |
| `to` | string (RFC 3339) | ✅ Да | Конечная дата (формат: 2019-08-24T14:15:22Z) |

### Response (HTTP 200)

```json
{
  "UUID": "string",
  "vendor": true
}
```

### Содержимое отчёта (CSV)

Отчёт содержит следующие данные:

| Поле | Описание |
|------|----------|
| Период отчёта | Временной промежуток |
| Дата | Дата заказа |
| ID заказа | Идентификатор заказа |
| Номер заказа | Номер заказа |
| SKU | SKU товара |
| SKU продвигаемого товара | SKU продвигаемого товара |
| Артикул | Артикул товара |
| Наименование | Название товара |
| Источник заказа | Источник заказа |
| Количество | Количество товаров |
| Стоимость, ₽ | Стоимость заказа |
| Ставка, % | Ставка в процентах |
| Ставка, ₽ | Ставка в рублях |
| Расход, ₽ | Расход на рекламу |

### JSON формат

Чтобы запросить отчёт в формате JSON:

```
POST /api/client/statistics/orders/generate/json
```

**⚠️ ОБРАТИТЕ ВНИМАНИЕ:** Для JSON формата путь `/statistics/` с "s".

### Статус эндпоинта

- ✅ **Работает** — эндпоинт для кампаний "Оплата за заказ"
- ⚠️ **Асинхронный** — возвращает UUID, требуется проверка статуса и скачивание
- 📊 **Детализация по заказам** — подробная информация о каждом заказе, SKU, ставках

---

## 📦 Эндпоинт для товаров "Оплата за заказ": POST /api/client/statistic/products/generate

### Описание

**⚠️ ВАЖНО:** Этот эндпоинт **АСИНХРОННЫЙ**! Возвращает UUID, нужно проверять статус и скачивать отчёт.

API для получения отчёта по товарам в кампаниях "Оплата за заказ" (SEARCH_PROMO).

**⚠️ ОБРАТИТЕ ВНИМАНИЕ:** В пути `/statistic/` без "s" (опечатка в API).

### Request Body (application/json)

```json
{
  "from": "2019-08-24T14:15:22Z",
  "to": "2019-08-24T14:15:22Z"
}
```

### Параметры запроса:

| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| `from` | string (RFC 3339) | ✅ Да | Начальная дата (формат: 2019-08-24T14:15:22Z) |
| `to` | string (RFC 3339) | ✅ Да | Конечная дата (формат: 2019-08-24T14:15:22Z) |

### Response (HTTP 200)

```json
{
  "UUID": "string",
  "vendor": true
}
```

### Содержимое отчёта (CSV)

Отчёт содержит следующие данные:

| Поле | Описание |
|------|----------|
| Период отчёта | Временной промежуток |
| SKU | SKU товара |
| Артикул | Артикул товара |
| Наименование | Название товара |
| Категория | Категория товара |
| Продвижение | Тип продвижения |
| Цена товара, ₽ | Цена товара |
| Ставка, % | Ставка в процентах |
| Ставка, ₽ | Ставка в рублях |
| Количество заказов | Число заказов |
| Сумма заказов, ₽ | Выручка от заказов |
| Расход, ₽ | Расход на рекламу |
| Продажи ("Оплата за клик"), ₽ | Продажи из кампаний "Оплата за клик" |
| Расход ("Оплата за клик"), ₽ | Расход из кампаний "Оплата за клик" |
| Заказы ("Оплата за клик"), шт | Заказы из кампаний "Оплата за клик" |
| В корзину | Добавления в корзину |
| ДРР, % | Доля рекламных расходов |
| Последнее изменение | Дата последнего изменения |

### JSON формат

Чтобы запросить отчёт в формате JSON:

```
POST /api/client/statistics/products/generate/json
```

**⚠️ ОБРАТИТЕ ВНИМАНИЕ:** Для JSON формата путь `/statistics/` с "s".

### Статус эндпоинта

- ✅ **Работает** — эндпоинт для товаров "Оплата за заказ"
- ⚠️ **Асинхронный** — возвращает UUID, требуется проверка статуса и скачивание
- 📊 **Комплексная статистика** — включает данные по обоим типам кампаний (оплата за заказ + оплата за клик)
- 💰 **ДРР и метрики** — подробные данные о ставках, расходах, продажах

---

## ⚠️ Что нужно изменить в коде

### Старый подход (устарел):
```python
# GET /api/client/statistics/expense
r = requests.get(expense_url, headers=headers, params=params)
csv_content = r.text  # Сразу получаем CSV
```

### Новый подход (с 20.01.2026):
```python
# 1. Отправить запрос
r = requests.post(
    "https://api-performance.ozon.ru/api/client/statistics",
    json={
        "campaigns": [campaign_id],
        "dateFrom": date_from,
        "dateTo": date_to,
        "groupBy": "DATE"
    },
    headers=headers
)

response_data = r.json()
uuid = response_data["UUID"]

# 2. Проверить статус формирования отчёта
# (нужен дополнительный эндпоинт для проверки статуса)

# 3. Скачать готовый отчёт по UUID
# (нужен дополнительный эндпоинт для скачивания)
```

---

## 🚨 TODO: Обновить функцию load_adv_spend_by_sku()

Текущая функция использует старый синхронный API. Необходимо переписать с учётом:

1. ✅ Асинхронного запроса (POST + UUID)
2. ✅ Проверки статуса готовности отчёта
3. ✅ Скачивания готового отчёта

**Дата изменений API:** 20.01.2026
**Требуется обновление кода:** ДА

---

## 📖 Ссылки на документацию

- Seller API: `.claude/ozon-api-docs/ozon-seller-api.*`
- Performance API: `.claude/ozon-api-docs/ozon-performance-api.*`
- Этот файл: `.claude/ozon-api-docs/performance-api-changes-2026-01-20.md`
